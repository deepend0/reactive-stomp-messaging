quarkus:
  http:
    test-port: 8091
  devservices:
    enabled: false

reactive-stomp:
  messaging:
    broker:
      regex: .*
    message-endpoint:
      regex: /messageEndpoint.*
  stomp:
    ackPeriod: 1000

kafka:
  bootstrap-servers: localhost:9092